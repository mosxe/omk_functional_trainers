import React from 'react';
import ReactDOM from 'react-dom/client';
import MainPage from './pages';
import './index.css';

ReactDOM.createRoot(document.getElementById('functional-trainers')!).render(
  <React.StrictMode>
    <MainPage />
  </React.StrictMode>
);
